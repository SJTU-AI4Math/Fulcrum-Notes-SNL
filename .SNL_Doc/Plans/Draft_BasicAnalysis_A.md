# Draft: BasicAnalysis A — Real Numbers and Limits of Sequences

本文件是 SNL-Doc 知识库 `Fulcrum-Notes-SNL` 的 Phase 1 起稿（Drafting），覆盖数学分析基础的前两大块：

1. **实数（Real Numbers）**：有序域公理、上确界/下确界、完备性（确界原理）、Archimedes 性质、有理数与无理数的稠密性、绝对值与三角不等式、区间与邻域。
2. **实数列（Sequences of Reals）**：数列极限的 ε–N 定义、极限唯一性、收敛蕴含有界、极限的四则运算与保序性、夹逼定理、单调有界定理、子列与 Bolzano–Weierstrass 定理、Cauchy 列与 Cauchy 收敛准则、上极限与下极限、无穷小量与无穷大量、常用重要极限、发散数列。

不覆盖：级数、函数极限、连续性、微分与积分（留给后续 Draft）。

约定：ID 前缀统一为 `BasicAnalysis`，格式 `<domain>.<kind>.<slug>`（点号分隔）。表格列与 entry json schema 对齐（`id` / `kind` / `title` / `content`），额外的 `parent` 列记录该条目在 Library `graph.json` 树中的父节点 id（`branch` 父→子），`section` 条目 parent 留空。表格行的顺序即阅读顺序（深度优先）。`content` 列为英文自然语言草稿，SNL 形式留待 Phase 2/3。类型论优先：集合是类型上的谓词 `Set(T) := T → Prop`，`x ∈ A := A(x)`，量词一律 typed。

| id | kind | title | parent | content |
|---|---|---|---|---|
| BasicAnalysis.sec.reals | section | The Real Numbers | | Axiomatic development of the ordered field ℝ: field and order axioms, the least-upper-bound (completeness) axiom, and the order-theoretic consequences (Archimedean property, density, absolute value, intervals and neighbourhoods) that the theory of limits rests on. |
| BasicAnalysis.ctxt.field | context | Context: a field with order | BasicAnalysis.sec.reals | Shared binders for the axiomatic subsection. Declares `F : Type` together with operations `add : F → F → F`, `mul : F → F → F`, distinguished elements `zero one : F`, and a binary relation `lt : F → F → Prop`. Element binders `x y z : F` are shared by all entries in `BasicAnalysis.subsec.axioms`. |
| BasicAnalysis.subsec.axioms | subsection | Ordered Field Axioms | BasicAnalysis.sec.reals | The algebraic and order axioms satisfied by ℝ, stated over the context `BasicAnalysis.ctxt.field`. |
| BasicAnalysis.def.field | definition | Field | BasicAnalysis.subsec.axioms | Uses `BasicAnalysis.ctxt.field`. `F` with `(add, mul, zero, one)` is a field iff: `add` is associative and commutative with identity `zero` and for every `x : F` an additive inverse `neg x` with `add x (neg x) = zero`; `mul` is associative and commutative with identity `one`, and every `x : F` with `x ≠ zero` admits `inv x` with `mul x (inv x) = one`; `mul` distributes over `add`, i.e. `mul x (add y z) = add (mul x y) (mul x z)`; and `one ≠ zero`. |
| BasicAnalysis.def.linear_order | definition | Linear Order | BasicAnalysis.subsec.axioms | Uses `BasicAnalysis.ctxt.field`. `lt : F → F → Prop` is a linear (total) order iff it is irreflexive (`¬ lt x x`), transitive (`lt x y → lt y z → lt x z`), and satisfies trichotomy: for all `x y : F` exactly one of `lt x y`, `x = y`, `lt y x` holds. Write `x < y := lt x y` and `x ≤ y := (x < y) ∨ (x = y)`. |
| BasicAnalysis.def.ordered_field | definition | Ordered Field | BasicAnalysis.subsec.axioms | Uses `BasicAnalysis.ctxt.field`. `(F, add, mul, zero, one, lt)` is an ordered field iff it is a field (`BasicAnalysis.def.field`), `lt` is a linear order (`BasicAnalysis.def.linear_order`), and the two compatibility axioms hold: (i) translation invariance, `x < y → add x z < add y z` for all `z : F`; (ii) positivity of products, `zero < x → zero < y → zero < mul x y`. |
| BasicAnalysis.property.ordered_field_basic | property | Elementary Consequences of the Order Axioms | BasicAnalysis.def.ordered_field | Uses `BasicAnalysis.ctxt.field` with `F` an ordered field. For all `x y z : F`: `x < y ↔ zero < add y (neg x)`; `x < y → neg y < neg x`; `zero < z → (x < y ↔ mul x z < mul y z)`; `z < zero → (x < y ↔ mul y z < mul x z)`; `x ≠ zero → zero < mul x x`; in particular `zero < one`; and `zero < x → zero < inv x`. |
| BasicAnalysis.def.bounded_above | definition | Upper Bound, Bounded Above | BasicAnalysis.subsec.axioms | Uses `BasicAnalysis.ctxt.field`. For `A : Set F` (i.e. `A : F → Prop`) and `b : F`, `b` is an upper bound of `A` iff `∀ x : F, A x → x ≤ b`. `A` is bounded above iff `∃ b : F, b` is an upper bound of `A`. Dually, `b` is a lower bound iff `∀ x : F, A x → b ≤ x`, and `A` is bounded below iff such `b` exists; `A` is bounded iff it is bounded above and below. |
| BasicAnalysis.def.supremum | definition | Supremum (Least Upper Bound) | BasicAnalysis.subsec.axioms | Uses `BasicAnalysis.ctxt.field`. For `A : Set F` and `s : F`, `s = sup A` iff (i) `s` is an upper bound of `A`, and (ii) `s` is least among upper bounds: `∀ b : F, (∀ x : F, A x → x ≤ b) → s ≤ b`. Equivalently (ii'): `∀ ε : F, zero < ε → ∃ x : F, A x ∧ add s (neg ε) < x`. |
| BasicAnalysis.def.infimum | definition | Infimum (Greatest Lower Bound) | BasicAnalysis.subsec.axioms | Uses `BasicAnalysis.ctxt.field`. For `A : Set F` and `i : F`, `i = inf A` iff `i` is a lower bound of `A` and `∀ b : F, (∀ x : F, A x → b ≤ x) → b ≤ i`. Equivalently, the second clause reads: `∀ ε : F, zero < ε → ∃ x : F, A x ∧ x < add i ε`. |
| BasicAnalysis.theorem.sup_unique | theorem | Uniqueness of the Supremum | BasicAnalysis.def.supremum | Uses `BasicAnalysis.ctxt.field`. For `A : Set F`, if `s₁` and `s₂` both satisfy `BasicAnalysis.def.supremum` for `A`, then `s₁ = s₂`. The same holds for infima. |
| BasicAnalysis.proof.sup_unique | proof | Proof of Uniqueness of the Supremum | BasicAnalysis.theorem.sup_unique | Since `s₁` is an upper bound and `s₂` is least among upper bounds, `s₂ ≤ s₁`. Symmetrically `s₁ ≤ s₂`. Antisymmetry of `≤` (from trichotomy in `BasicAnalysis.def.linear_order`) gives `s₁ = s₂`. |
| BasicAnalysis.axiom.completeness | axiom | Completeness Axiom (Least Upper Bound Property) | BasicAnalysis.subsec.axioms | Uses `BasicAnalysis.ctxt.field`. `F` is order-complete iff every `A : Set F` that is nonempty (`∃ x : F, A x`) and bounded above admits a supremum in `F`: `∃ s : F, s = sup A` in the sense of `BasicAnalysis.def.supremum`. This axiom fails in ℚ and is what distinguishes ℝ. |
| BasicAnalysis.def.reals | definition | The Real Numbers ℝ | BasicAnalysis.subsec.axioms | ℝ is defined as a complete ordered field: a type `ℝ` with `(add, mul, 0, 1, <)` satisfying `BasicAnalysis.def.ordered_field` together with `BasicAnalysis.axiom.completeness`. All subsequent entries in `BasicAnalysis` work over this fixed structure. |
| BasicAnalysis.theorem.reals_unique | theorem | Uniqueness of ℝ up to Order-Field Isomorphism | BasicAnalysis.def.reals | Any two complete ordered fields `F₁` and `F₂` are isomorphic: there exists a bijection `φ : F₁ → F₂` with `φ (add x y) = add (φ x) (φ y)`, `φ (mul x y) = mul (φ x) (φ y)`, and `x < y ↔ φ x < φ y`. Moreover such `φ` is unique. |
| BasicAnalysis.remark.reals_construction | remark | Remark: Constructions of ℝ | BasicAnalysis.def.reals | Intuition and provenance. The axioms above are realised by concrete constructions from ℚ: Dedekind cuts (a real is a downward-closed nonempty proper subset of ℚ without a greatest element, so completeness is nearly definitional) and Cauchy-sequence completion (ℝ is the quotient of Cauchy sequences of rationals by the null-difference equivalence, so completeness appears as Cauchy completeness). By `BasicAnalysis.theorem.reals_unique` the choice is immaterial for analysis. |
| BasicAnalysis.theorem.inf_from_sup | theorem | Greatest Lower Bound Property | BasicAnalysis.axiom.completeness | In ℝ, every nonempty `A : Set ℝ` bounded below admits an infimum. |
| BasicAnalysis.proof.inf_from_sup | proof | Proof of the Greatest Lower Bound Property | BasicAnalysis.theorem.inf_from_sup | Let `B := { b : ℝ | ∀ x : ℝ, A x → b ≤ x }` be the set of lower bounds of `A`. `B` is nonempty by hypothesis and bounded above by any element of `A`. By `BasicAnalysis.axiom.completeness` put `s := sup B`. Every `x` with `A x` is an upper bound of `B`, hence `s ≤ x`, so `s ∈ B`; being the largest element of `B`, `s = inf A`. |
| BasicAnalysis.subsec.arch | subsection | Archimedean Property and Density | BasicAnalysis.sec.reals | Consequences of completeness governing how ℕ, ℤ and ℚ sit inside ℝ. |
| BasicAnalysis.theorem.archimedes | theorem | Archimedean Property | BasicAnalysis.subsec.arch | For every `x : ℝ` there exists `n : ℕ` with `x < n` (identifying `n : ℕ` with its image in ℝ under the canonical embedding). Equivalently: for all `x y : ℝ` with `0 < x`, there exists `n : ℕ` with `y < n · x`; equivalently `∀ ε : ℝ, 0 < ε → ∃ n : ℕ, 1 / (n + 1) < ε`. |
| BasicAnalysis.proof.archimedes | proof | Proof of the Archimedean Property | BasicAnalysis.theorem.archimedes | Suppose ℕ (as a subset of ℝ) were bounded above. It is nonempty, so by `BasicAnalysis.axiom.completeness` `s := sup ℕ` exists. Then `s − 1` is not an upper bound, so there is `n : ℕ` with `s − 1 < n`, whence `s < n + 1 ∈ ℕ`, contradicting that `s` is an upper bound. Hence ℕ is unbounded above, which is the statement. |
| BasicAnalysis.remark.non_archimedean | remark | Remark: Ordered Fields Failing Archimedes | BasicAnalysis.theorem.archimedes | The Archimedean property is not an order-field theorem: the field of rational functions ℝ(t) ordered by eventual sign at `+∞` is an ordered field in which `t` exceeds every integer. Thus `BasicAnalysis.theorem.archimedes` genuinely consumes completeness, and infinitesimals (`0 < ε < 1/n` for all `n`) are impossible in ℝ. |
| BasicAnalysis.theorem.floor | theorem | Existence of the Integer Part | BasicAnalysis.subsec.arch | For every `x : ℝ` there is a unique `m : ℤ` with `m ≤ x < m + 1`. This `m` is written `⌊x⌋`. |
| BasicAnalysis.theorem.q_dense | theorem | Density of ℚ in ℝ | BasicAnalysis.subsec.arch | For all `x y : ℝ` with `x < y` there exists `q : ℚ` with `x < q < y`. |
| BasicAnalysis.proof.q_dense | proof | Proof of the Density of ℚ | BasicAnalysis.theorem.q_dense | By `BasicAnalysis.theorem.archimedes` choose `n : ℕ` with `1 / n < y − x`, i.e. `n · (y − x) > 1`. Put `m := ⌊n x⌋ + 1` using `BasicAnalysis.theorem.floor`, so `m − 1 ≤ n x < m`. Then `n x < m ≤ n x + 1 < n y`, and dividing by `n > 0` gives `x < m / n < y`; take `q := m / n`. |
| BasicAnalysis.theorem.irr_dense | theorem | Density of the Irrationals | BasicAnalysis.subsec.arch | For all `x y : ℝ` with `x < y` there exists `z : ℝ` with `x < z < y` and `z ∉ ℚ`. Proof sketch: apply `BasicAnalysis.theorem.q_dense` to `x − √2 < y − √2` to get `q : ℚ`, then take `z := q + √2`. |
| BasicAnalysis.theorem.sqrt2_exists | theorem | Existence of √2 | BasicAnalysis.subsec.arch | There exists a unique `x : ℝ` with `0 < x` and `x · x = 2`; moreover `x ∉ ℚ`. More generally, for every `y : ℝ` with `0 ≤ y` and every `n : ℕ` with `n ≥ 1` there is a unique `x : ℝ` with `0 ≤ x` and `xⁿ = y`. |
| BasicAnalysis.proof.sqrt2_exists | proof | Proof of the Existence of √2 | BasicAnalysis.theorem.sqrt2_exists | Let `A := { t : ℝ | 0 ≤ t ∧ t · t < 2 }`. `A` is nonempty (`1 ∈ A`) and bounded above by `2`, so `x := sup A` exists by `BasicAnalysis.axiom.completeness`. If `x · x < 2`, choosing `h := min(1, (2 − x²)/(2x + 1)) > 0` gives `(x + h)² < 2`, contradicting that `x` is an upper bound. If `x · x > 2`, choosing `h := (x² − 2)/(2x) > 0` gives `(x − h)² > 2`, so `x − h` is a smaller upper bound, contradicting minimality. Hence `x · x = 2`. Irrationality follows from the standard parity argument on a reduced fraction `p/q`. |
| BasicAnalysis.subsec.abs | subsection | Absolute Value, Intervals, Neighbourhoods | BasicAnalysis.sec.reals | The metric vocabulary on ℝ used throughout the ε–N and ε–δ formalism. |
| BasicAnalysis.def.abs | definition | Absolute Value | BasicAnalysis.subsec.abs | For `x : ℝ`, `abs x := if 0 ≤ x then x else −x`; written `\|x\|`. Characterising property: `\|x\| = max(x, −x)`, hence `0 ≤ \|x\|`, `\|x\| = 0 ↔ x = 0`, and for `a : ℝ` with `0 ≤ a`, `\|x\| ≤ a ↔ (−a ≤ x ∧ x ≤ a)`. |
| BasicAnalysis.property.abs_mult | property | Multiplicativity of the Absolute Value | BasicAnalysis.def.abs | For all `x y : ℝ`: `\|x · y\| = \|x\| · \|y\|`, and for `y ≠ 0`, `\|x / y\| = \|x\| / \|y\|`. Also `\|−x\| = \|x\|` and `\|x\|² = x²`. |
| BasicAnalysis.theorem.triangle | theorem | Triangle Inequality | BasicAnalysis.def.abs | For all `x y : ℝ`: `\|x + y\| ≤ \|x\| + \|y\|`. Variants: the reverse triangle inequality `\| \|x\| − \|y\| \| ≤ \|x − y\|`, and the finite form `\|Σ_{k=1}^{n} x_k\| ≤ Σ_{k=1}^{n} \|x_k\|`. |
| BasicAnalysis.proof.triangle | proof | Proof of the Triangle Inequality | BasicAnalysis.theorem.triangle | From `−\|x\| ≤ x ≤ \|x\|` and `−\|y\| ≤ y ≤ \|y\|`, adding gives `−(\|x\|+\|y\|) ≤ x + y ≤ \|x\|+\|y\|`, and the characterisation in `BasicAnalysis.def.abs` yields `\|x+y\| ≤ \|x\|+\|y\|`. For the reverse form apply this to `x = (x − y) + y` to get `\|x\| − \|y\| ≤ \|x − y\|` and symmetrise. |
| BasicAnalysis.def.distance | definition | Distance on ℝ | BasicAnalysis.subsec.abs | `d : ℝ → ℝ → ℝ`, `d x y := \|x − y\|`. It satisfies the metric axioms: `d x y ≥ 0`, `d x y = 0 ↔ x = y`, `d x y = d y x`, and `d x z ≤ d x y + d y z` (from `BasicAnalysis.theorem.triangle`). |
| BasicAnalysis.def.interval | definition | Intervals | BasicAnalysis.subsec.abs | For `a b : ℝ`: `(a,b) := { x : ℝ \| a < x ∧ x < b }`, `[a,b] := { x : ℝ \| a ≤ x ∧ x ≤ b }`, and the half-open `[a,b)`, `(a,b]` analogously; unbounded intervals `(a,+∞) := { x \| a < x }`, `(−∞,b) := { x \| x < b }`, with closed variants. As predicates these are elements of `Set ℝ := ℝ → Prop`. |
| BasicAnalysis.def.neighbourhood | definition | ε-Neighbourhood and Punctured Neighbourhood | BasicAnalysis.subsec.abs | For `x₀ : ℝ` and `ε : ℝ` with `0 < ε`, `U(x₀, ε) := { x : ℝ \| \|x − x₀\| < ε } = (x₀ − ε, x₀ + ε)`. The punctured neighbourhood is `U°(x₀, ε) := { x : ℝ \| 0 < \|x − x₀\| < ε }`. Neighbourhoods of `+∞`: `{ x \| x > M }` for `M : ℝ`; of `−∞`: `{ x \| x < −M }`; of `∞`: `{ x \| \|x\| > M }`. |
| BasicAnalysis.remark.eps_language | remark | Remark: Why ε-Neighbourhoods | BasicAnalysis.def.neighbourhood | Intuition. Every ε–N or ε–δ statement is a statement about neighbourhoods: "eventually inside every neighbourhood of `L`". Rephrasing limits in the neighbourhood language is what generalises verbatim to metric and topological spaces, whereas the inequality `\|a_n − L\| < ε` is ℝ-specific bookkeeping. |
| BasicAnalysis.sec.seq | section | Sequences of Real Numbers | | The theory of convergence for sequences `a : ℕ → ℝ`: the ε–N definition and its immediate consequences, the algebra and order theory of limits, the four classical completeness theorems (monotone convergence, Bolzano–Weierstrass, Cauchy criterion, nested intervals), limsup/limstinf, infinitesimals and infinities, standard limits, and divergence. |
| BasicAnalysis.ctxt.seq | context | Context: Sequences and Their Limits | BasicAnalysis.sec.seq | Parent context for the whole section. Declares `a b : ℕ → ℝ` (real sequences, written `a_n := a n`), `L M : ℝ` (candidate limits), `ε : ℝ` with `0 < ε` (tolerance binder), `N n m : ℕ` (index binders), and `c : ℝ` (scalar). All entries under `BasicAnalysis.sec.seq` referring to `a`, `L`, `ε`, `N` refer to these binders. |
| BasicAnalysis.ctxt.seq.index | context | Context: Index Extraction | BasicAnalysis.ctxt.seq | Declares `k : ℕ → ℕ` a strictly increasing index map (`∀ p q : ℕ, p < q → k p < k q`), used to form subsequences `a ∘ k : ℕ → ℝ`, written `a_{n_k}`. Referenced by `BasicAnalysis.def.subsequence` and everything downstream of it. |
| BasicAnalysis.subsec.limit | subsection | The Limit of a Sequence | BasicAnalysis.sec.seq | The ε–N definition, uniqueness, boundedness, and the eventually/frequently vocabulary. |
| BasicAnalysis.def.sequence | definition | Real Sequence | BasicAnalysis.subsec.limit | A real sequence is a function `a : ℕ → ℝ`; the value at `n` is written `a_n` and the whole object `(a_n)_{n∈ℕ}`. A sequence is not its image set: `(a_n) = ((−1)^n)` has two-element image `{−1, 1}` but is a distinct object from any enumeration of that set. |
| BasicAnalysis.def.eventually | definition | Eventually and Frequently | BasicAnalysis.subsec.limit | Uses `BasicAnalysis.ctxt.seq`. For a predicate `P : ℕ → Prop`: `P` holds *eventually* iff `∃ N : ℕ, ∀ n : ℕ, n ≥ N → P n`; `P` holds *frequently* (infinitely often) iff `∀ N : ℕ, ∃ n : ℕ, n ≥ N ∧ P n`. These are dual: `¬(eventually P) ↔ frequently (¬P)`. |
| BasicAnalysis.def.limit | definition | Limit of a Sequence (ε–N) | BasicAnalysis.subsec.limit | Uses `BasicAnalysis.ctxt.seq`. `a` converges to `L`, written `lim_{n→∞} a_n = L` or `a_n → L`, iff `∀ ε : ℝ, 0 < ε → ∃ N : ℕ, ∀ n : ℕ, n ≥ N → \|a_n − L\| < ε`. Equivalently, using `BasicAnalysis.def.neighbourhood` and `BasicAnalysis.def.eventually`: for every `ε > 0`, `a_n ∈ U(L, ε)` eventually. `a` is convergent iff `∃ L : ℝ, a_n → L`; otherwise divergent. |
| BasicAnalysis.remark.limit_quantifier | remark | Remark: Reading the ε–N Quantifier Block | BasicAnalysis.def.limit | Intuition. The alternation `∀ε ∃N ∀n≥N` is an adversarial game: the opponent picks the tolerance `ε` first, and only then may one choose the threshold `N`, so `N` may depend on `ε` but never the reverse. Swapping to `∃N ∀ε` gives the far stronger statement `a_n = L` eventually. Replacing `< ε` by `≤ ε`, or `0 < ε` by `0 < ε < 1`, or `n ≥ N` by `n > N`, all yield equivalent definitions. |
| BasicAnalysis.example.limit_one_over_n | example | Example: `1/n → 0` | BasicAnalysis.def.limit | Uses `BasicAnalysis.ctxt.seq` with `a_n := 1/(n+1)`. Given `ε > 0`, `BasicAnalysis.theorem.archimedes` supplies `N : ℕ` with `1/(N+1) < ε`; for `n ≥ N` we get `\|a_n − 0\| = 1/(n+1) ≤ 1/(N+1) < ε`. Hence `lim 1/(n+1) = 0`. |
| BasicAnalysis.example.limit_geometric | example | Example: Geometric Sequence Tends to Zero | BasicAnalysis.def.limit | Let `q : ℝ` with `\|q\| < 1`. Write `\|q\| = 1/(1+h)` with `h > 0`. Bernoulli's inequality gives `(1+h)ⁿ ≥ 1 + n h`, so `\|qⁿ − 0\| ≤ 1/(1 + n h)`. Given `ε > 0` pick `N > (1/ε − 1)/h` by `BasicAnalysis.theorem.archimedes`; then `n ≥ N` implies `\|qⁿ\| < ε`. |
| BasicAnalysis.counterexample.limit_alternating | counterexample | Counterexample: `(−1)ⁿ` Has No Limit | BasicAnalysis.def.limit | Suppose `(−1)ⁿ → L`. Take `ε := 1`. Then there is `N` with `\|(−1)ⁿ − L\| < 1` for all `n ≥ N`; applying this at some even `n ≥ N` and some odd `n+1` gives `2 = \|1 − (−1)\| ≤ \|1 − L\| + \|L − (−1)\| < 2`, a contradiction. Hence the sequence is divergent although bounded. |
| BasicAnalysis.theorem.limit_unique | theorem | Uniqueness of the Limit | BasicAnalysis.def.limit | Uses `BasicAnalysis.ctxt.seq`. If `a_n → L` and `a_n → M` then `L = M`. |
| BasicAnalysis.proof.limit_unique | proof | Proof of Uniqueness of the Limit | BasicAnalysis.theorem.limit_unique | Assume `L ≠ M` and set `ε := \|L − M\| / 2 > 0`. Choose `N₁` with `\|a_n − L\| < ε` for `n ≥ N₁` and `N₂` with `\|a_n − M\| < ε` for `n ≥ N₂`. For `n := max(N₁, N₂)`, `BasicAnalysis.theorem.triangle` gives `\|L − M\| ≤ \|L − a_n\| + \|a_n − M\| < 2ε = \|L − M\|`, a contradiction. |
| BasicAnalysis.def.bounded_seq | definition | Bounded Sequence | BasicAnalysis.subsec.limit | Uses `BasicAnalysis.ctxt.seq`. `a` is bounded iff `∃ M : ℝ, ∀ n : ℕ, \|a_n\| ≤ M`; bounded above iff `∃ M : ℝ, ∀ n, a_n ≤ M`; bounded below iff `∃ m : ℝ, ∀ n, m ≤ a_n`. Equivalently, the image predicate `{ x : ℝ \| ∃ n, x = a_n } : Set ℝ` is bounded in the sense of `BasicAnalysis.def.bounded_above`. |
| BasicAnalysis.theorem.conv_implies_bounded | theorem | Convergent Sequences Are Bounded | BasicAnalysis.def.limit | Uses `BasicAnalysis.ctxt.seq`. If `a` converges then `a` is bounded. The converse fails by `BasicAnalysis.counterexample.limit_alternating`. |
| BasicAnalysis.proof.conv_implies_bounded | proof | Proof that Convergent Sequences Are Bounded | BasicAnalysis.theorem.conv_implies_bounded | Let `a_n → L`. With `ε := 1` obtain `N` such that `\|a_n − L\| < 1`, hence `\|a_n\| < \|L\| + 1`, for all `n ≥ N`. Put `M := max(\|a_0\|, …, \|a_{N−1}\|, \|L\| + 1)`, a maximum over a finite index set, so `\|a_n\| ≤ M` for every `n`. |
| BasicAnalysis.theorem.limit_local_sign | theorem | Sign Preservation | BasicAnalysis.def.limit | Uses `BasicAnalysis.ctxt.seq`. If `a_n → L` and `L > 0`, then `a_n > L/2 > 0` eventually. Consequently if `a_n ≥ 0` for all `n` and `a_n → L` then `L ≥ 0`; note that strict inequalities are not preserved: `a_n := 1/(n+1) > 0` but `L = 0`. |
| BasicAnalysis.theorem.limit_order | theorem | Order Preservation in the Limit | BasicAnalysis.def.limit | Uses `BasicAnalysis.ctxt.seq`. If `a_n → L`, `b_n → M`, and `a_n ≤ b_n` eventually, then `L ≤ M`. Conversely, if `L < M` then `a_n < b_n` eventually. |
| BasicAnalysis.subsec.algebra | subsection | Algebra of Limits | BasicAnalysis.sec.seq | Compatibility of the limit operator with the field operations, order, and the squeeze principle. |
| BasicAnalysis.theorem.limit_add | theorem | Limit of a Sum and Scalar Multiple | BasicAnalysis.subsec.algebra | Uses `BasicAnalysis.ctxt.seq`. If `a_n → L` and `b_n → M` then `a_n + b_n → L + M` and `c · a_n → c · L` for every `c : ℝ`; in particular `a_n − b_n → L − M`. Thus the set of convergent sequences is an ℝ-subspace of `ℕ → ℝ` and `lim` is ℝ-linear on it. |
| BasicAnalysis.proof.limit_add | proof | Proof of the Sum Rule | BasicAnalysis.theorem.limit_add | Given `ε > 0`, choose `N₁` with `\|a_n − L\| < ε/2` for `n ≥ N₁` and `N₂` with `\|b_n − M\| < ε/2` for `n ≥ N₂`. For `n ≥ max(N₁,N₂)`, `BasicAnalysis.theorem.triangle` yields `\|(a_n + b_n) − (L + M)\| ≤ \|a_n − L\| + \|b_n − M\| < ε`. |
| BasicAnalysis.theorem.limit_mul | theorem | Limit of a Product | BasicAnalysis.subsec.algebra | Uses `BasicAnalysis.ctxt.seq`. If `a_n → L` and `b_n → M` then `a_n · b_n → L · M`. |
| BasicAnalysis.proof.limit_mul | proof | Proof of the Product Rule | BasicAnalysis.theorem.limit_mul | Write `a_n b_n − L M = a_n (b_n − M) + M (a_n − L)`. By `BasicAnalysis.theorem.conv_implies_bounded` fix `K > 0` with `\|a_n\| ≤ K` for all `n`, and enlarge `K` so that `\|M\| ≤ K`. Given `ε > 0`, choose `N` beyond which `\|b_n − M\| < ε/(2K)` and `\|a_n − L\| < ε/(2K)`; then `\|a_n b_n − L M\| ≤ K\|b_n − M\| + K\|a_n − L\| < ε`. |
| BasicAnalysis.theorem.limit_div | theorem | Limit of a Quotient | BasicAnalysis.subsec.algebra | Uses `BasicAnalysis.ctxt.seq`. If `a_n → L`, `b_n → M`, `M ≠ 0`, and `b_n ≠ 0` for all `n`, then `a_n / b_n → L / M`. Key step: by `BasicAnalysis.theorem.limit_local_sign` applied to `\|b_n\|`, there is `N` with `\|b_n\| > \|M\|/2` for `n ≥ N`, so `\|1/b_n − 1/M\| = \|M − b_n\| / (\|b_n\|\|M\|) ≤ 2\|M − b_n\| / \|M\|²`. |
| BasicAnalysis.theorem.squeeze | theorem | Squeeze Theorem | BasicAnalysis.subsec.algebra | Uses `BasicAnalysis.ctxt.seq`. Let `a b c : ℕ → ℝ` with `a_n ≤ b_n ≤ c_n` eventually. If `a_n → L` and `c_n → L` then `b_n → L`. |
| BasicAnalysis.proof.squeeze | proof | Proof of the Squeeze Theorem | BasicAnalysis.theorem.squeeze | Given `ε > 0` choose `N` beyond which the sandwich holds together with `L − ε < a_n` and `c_n < L + ε`. Then `L − ε < a_n ≤ b_n ≤ c_n < L + ε`, i.e. `\|b_n − L\| < ε`. |
| BasicAnalysis.corollary.abs_limit | corollary | Limit of the Absolute Value | BasicAnalysis.theorem.squeeze | If `a_n → L` then `\|a_n\| → \|L\|`, since `\| \|a_n\| − \|L\| \| ≤ \|a_n − L\|` by the reverse triangle inequality of `BasicAnalysis.theorem.triangle`. The converse holds only for `L = 0`: `a_n → 0 ↔ \|a_n\| → 0`; for `a_n := (−1)ⁿ`, `\|a_n\| → 1` while `a_n` diverges. |
| BasicAnalysis.corollary.limit_root | corollary | Limits and Powers/Roots | BasicAnalysis.theorem.limit_mul | If `a_n → L` then `a_n^k → L^k` for every fixed `k : ℕ` (induction on `k` via `BasicAnalysis.theorem.limit_mul`); and if additionally `a_n ≥ 0` then `a_n^{1/k} → L^{1/k}`. |
| BasicAnalysis.subsec.completeness | subsection | The Completeness Theorems for Sequences | BasicAnalysis.sec.seq | Monotone convergence, nested intervals, Bolzano–Weierstrass and the Cauchy criterion — four mutually equivalent formulations of the completeness of ℝ at the level of sequences. |
| BasicAnalysis.def.monotone | definition | Monotone Sequence | BasicAnalysis.subsec.completeness | Uses `BasicAnalysis.ctxt.seq`. `a` is increasing iff `∀ n : ℕ, a_n ≤ a_{n+1}`; strictly increasing iff `a_n < a_{n+1}`; decreasing and strictly decreasing dually. `a` is monotone iff it is increasing or decreasing. |
| BasicAnalysis.theorem.monotone_convergence | theorem | Monotone Convergence Theorem | BasicAnalysis.def.monotone | Uses `BasicAnalysis.ctxt.seq`. If `a` is increasing and bounded above, then `a` converges and `lim a_n = sup { x : ℝ \| ∃ n, x = a_n }`. Dually, a decreasing sequence bounded below converges to the infimum of its image. Consequently every monotone sequence in ℝ either converges or is unbounded (diverging to `±∞`). |
| BasicAnalysis.proof.monotone_convergence | proof | Proof of the Monotone Convergence Theorem | BasicAnalysis.theorem.monotone_convergence | Let `A := { x : ℝ \| ∃ n, x = a_n }`, nonempty and bounded above; by `BasicAnalysis.axiom.completeness` set `L := sup A`. Given `ε > 0`, `L − ε` is not an upper bound, so there is `N` with `a_N > L − ε`. Monotonicity gives `L − ε < a_N ≤ a_n ≤ L` for all `n ≥ N`, hence `\|a_n − L\| < ε`. |
| BasicAnalysis.example.euler_e | example | Example: The Sequence `(1 + 1/n)ⁿ` | BasicAnalysis.theorem.monotone_convergence | Uses `BasicAnalysis.ctxt.seq` with `a_n := (1 + 1/n)ⁿ` for `n ≥ 1`. By the AM–GM inequality `a_n` is strictly increasing, and `b_n := (1 + 1/n)^{n+1}` is strictly decreasing with `a_n < b_n ≤ b_1 = 4`. Hence `a` is increasing and bounded above, so by `BasicAnalysis.theorem.monotone_convergence` it converges; its limit is defined to be `e`, and `2 < e < 4` (in fact `e ≈ 2.71828`, irrational). |
| BasicAnalysis.theorem.nested_intervals | theorem | Nested Interval Theorem | BasicAnalysis.subsec.completeness | Let `I_n := [a_n, b_n]` be closed bounded intervals with `I_{n+1} ⊆ I_n` for all `n`. Then `⋂_{n∈ℕ} I_n ≠ ∅`. If moreover `b_n − a_n → 0`, the intersection is a single point `ξ`, and `a_n → ξ`, `b_n → ξ`. Closedness and boundedness are both necessary: `(0, 1/n)` and `[n, +∞)` have empty intersection. |
| BasicAnalysis.proof.nested_intervals | proof | Proof of the Nested Interval Theorem | BasicAnalysis.theorem.nested_intervals | Nesting makes `a` increasing and `b` decreasing, with `a_n ≤ b_m` for all `n, m`. So `a` is bounded above by `b_0`; put `ξ := sup { a_n }`, which exists by `BasicAnalysis.axiom.completeness`. Each `b_m` is an upper bound of `{a_n}`, hence `a_n ≤ ξ ≤ b_n` for all `n`, i.e. `ξ ∈ ⋂ I_n`. If `b_n − a_n → 0` and `ξ, ξ' ∈ ⋂ I_n` then `\|ξ − ξ'\| ≤ b_n − a_n → 0`, so `ξ = ξ'`; the squeeze theorem `BasicAnalysis.theorem.squeeze` gives `a_n → ξ` and `b_n → ξ`. |
| BasicAnalysis.def.subsequence | definition | Subsequence | BasicAnalysis.subsec.completeness | Uses `BasicAnalysis.ctxt.seq` and `BasicAnalysis.ctxt.seq.index`. Given `a : ℕ → ℝ` and a strictly increasing `k : ℕ → ℕ`, the subsequence is the composite `a ∘ k : ℕ → ℝ`, written `(a_{n_k})_{k∈ℕ}` with `n_k := k(k)`. Strict monotonicity forces `k(j) ≥ j` for all `j`, which is the workhorse inequality in subsequence proofs. |
| BasicAnalysis.theorem.subseq_limit | theorem | Subsequences Inherit the Limit | BasicAnalysis.def.subsequence | Uses `BasicAnalysis.ctxt.seq`, `BasicAnalysis.ctxt.seq.index`. If `a_n → L` then `a_{n_k} → L` for every strictly increasing `k`. Contrapositive test for divergence: if two subsequences of `a` converge to different limits, `a` diverges. Conversely, if every subsequence of `a` has a further subsequence converging to `L`, then `a_n → L`. |
| BasicAnalysis.proof.subseq_limit | proof | Proof that Subsequences Inherit the Limit | BasicAnalysis.theorem.subseq_limit | Given `ε > 0` take `N` with `\|a_n − L\| < ε` for `n ≥ N`. Since `k(j) ≥ j`, for `j ≥ N` we have `n_j = k(j) ≥ N`, hence `\|a_{n_j} − L\| < ε`. |
| BasicAnalysis.lemma.monotone_subseq | lemma | Every Sequence Has a Monotone Subsequence | BasicAnalysis.subsec.completeness | Uses `BasicAnalysis.ctxt.seq`. Every `a : ℕ → ℝ` admits a monotone subsequence. Proof idea: call `m : ℕ` a *peak* iff `∀ n > m, a_n ≤ a_m`. If there are infinitely many peaks, enumerating them gives a decreasing subsequence; if there are finitely many, past the last peak one can always choose a strictly larger term, yielding an increasing subsequence. |
| BasicAnalysis.theorem.bolzano_weierstrass | theorem | Bolzano–Weierstrass Theorem | BasicAnalysis.subsec.completeness | Uses `BasicAnalysis.ctxt.seq`. Every bounded sequence `a : ℕ → ℝ` admits a convergent subsequence. Set form: every infinite bounded `A : Set ℝ` has an accumulation point in ℝ. |
| BasicAnalysis.proof.bolzano_weierstrass | proof | Proof of Bolzano–Weierstrass | BasicAnalysis.theorem.bolzano_weierstrass | By `BasicAnalysis.lemma.monotone_subseq` extract a monotone subsequence; it is bounded because `a` is, so `BasicAnalysis.theorem.monotone_convergence` makes it convergent. Alternative bisection proof: start with `[A, B] ⊇ image(a)`, repeatedly halve, keeping at each step a half containing `a_n` for infinitely many `n`; `BasicAnalysis.theorem.nested_intervals` with lengths `(B−A)/2ⁿ → 0` gives a point `ξ`, and choosing indices `n_0 < n_1 < …` with `a_{n_k} ∈ I_k` yields `a_{n_k} → ξ`. |
| BasicAnalysis.def.cluster_point | definition | Cluster Point of a Sequence | BasicAnalysis.subsec.completeness | Uses `BasicAnalysis.ctxt.seq`. `L : ℝ` is a cluster (subsequential limit) point of `a` iff `∀ ε : ℝ, 0 < ε → ∀ N : ℕ, ∃ n ≥ N, \|a_n − L\| < ε`; i.e. `a_n ∈ U(L, ε)` frequently in the sense of `BasicAnalysis.def.eventually`. Equivalently, `L` is a cluster point iff some subsequence of `a` converges to `L`. A sequence converges to `L` iff it is bounded and `L` is its only cluster point. |
| BasicAnalysis.def.cauchy | definition | Cauchy Sequence | BasicAnalysis.subsec.completeness | Uses `BasicAnalysis.ctxt.seq`. `a` is Cauchy iff `∀ ε : ℝ, 0 < ε → ∃ N : ℕ, ∀ m n : ℕ, m ≥ N → n ≥ N → \|a_m − a_n\| < ε`. Equivalent form: `∀ ε > 0, ∃ N, ∀ n ≥ N, ∀ p : ℕ, \|a_{n+p} − a_n\| < ε`. Note that the pointwise-in-`p` weakening `∀ p fixed, \|a_{n+p} − a_n\| → 0` is strictly weaker and does not imply Cauchy. |
| BasicAnalysis.counterexample.cauchy_pointwise | counterexample | Counterexample: Vanishing Consecutive Gaps Do Not Imply Cauchy | BasicAnalysis.def.cauchy | Take `a_n := Σ_{j=1}^{n} 1/j` (harmonic partial sums). Then `a_{n+1} − a_n = 1/(n+1) → 0`, yet `a_{2n} − a_n ≥ 1/2` for every `n`, so `a` fails the Cauchy condition of `BasicAnalysis.def.cauchy` at `ε = 1/2` and diverges. |
| BasicAnalysis.theorem.cauchy_criterion | theorem | Cauchy Convergence Criterion | BasicAnalysis.def.cauchy | Uses `BasicAnalysis.ctxt.seq`. `a : ℕ → ℝ` converges iff `a` is Cauchy. (ℝ is a complete metric space with the distance of `BasicAnalysis.def.distance`.) The corresponding statement in ℚ is false, e.g. the decimal truncations of `√2`. |
| BasicAnalysis.proof.cauchy_criterion | proof | Proof of the Cauchy Criterion | BasicAnalysis.theorem.cauchy_criterion | (⇒) If `a_n → L`, given `ε > 0` take `N` with `\|a_n − L\| < ε/2` for `n ≥ N`; then `\|a_m − a_n\| ≤ \|a_m − L\| + \|L − a_n\| < ε` for `m, n ≥ N`. (⇐) A Cauchy sequence is bounded (apply the definition at `ε = 1` and take a maximum over the finitely many earlier terms), so by `BasicAnalysis.theorem.bolzano_weierstrass` some subsequence satisfies `a_{n_k} → L`. Given `ε > 0` pick `N` from the Cauchy condition at `ε/2` and `k` with `n_k ≥ N` and `\|a_{n_k} − L\| < ε/2`; then `n ≥ N` gives `\|a_n − L\| ≤ \|a_n − a_{n_k}\| + \|a_{n_k} − L\| < ε`. |
| BasicAnalysis.remark.completeness_equivalences | remark | Remark: Seven Equivalent Forms of Completeness | BasicAnalysis.subsec.completeness | Intuition and architecture. Over an Archimedean ordered field the following are mutually equivalent: the least-upper-bound axiom (`BasicAnalysis.axiom.completeness`), the monotone convergence theorem, the nested interval theorem, Bolzano–Weierstrass, the Cauchy criterion, the Heine–Borel finite-subcover property, and the existence of accumulation points for infinite bounded sets. Choosing any one as an axiom derives the rest; the standard cycle is LUB ⇒ monotone ⇒ nested ⇒ BW ⇒ Cauchy ⇒ LUB. |
| BasicAnalysis.subsec.limsup | subsection | Upper and Lower Limits | BasicAnalysis.sec.seq | `limsup` and `liminf` as the always-defined extended-real substitutes for `lim`. |
| BasicAnalysis.def.extended_reals | definition | Extended Real Line | BasicAnalysis.subsec.limsup | `ℝ̄ := ℝ ⊎ {−∞, +∞}` with the order extended by `−∞ < x < +∞` for every `x : ℝ`, and the partial arithmetic `x + (+∞) = +∞` for `x ≠ −∞`, `x · (+∞) = +∞` for `x > 0`, `x / (±∞) = 0`; the expressions `(+∞) + (−∞)`, `0 · (±∞)`, `(±∞)/(±∞)` remain undefined. In ℝ̄ every subset has a supremum and an infimum, with `sup ∅ = −∞`, `inf ∅ = +∞`. |
| BasicAnalysis.def.limsup | definition | Upper Limit (limsup) | BasicAnalysis.subsec.limsup | Uses `BasicAnalysis.ctxt.seq`. Put `s_N := sup { a_n : ℝ \| n ≥ N } : ℝ̄`. The sequence `(s_N)` is decreasing, so define `limsup_{n→∞} a_n := inf_{N∈ℕ} s_N = lim_{N→∞} sup_{n ≥ N} a_n : ℝ̄`. Characterisation for `L : ℝ`: `limsup a_n = L` iff for every `ε > 0`, `a_n < L + ε` eventually and `a_n > L − ε` frequently (`BasicAnalysis.def.eventually`). |
| BasicAnalysis.def.liminf | definition | Lower Limit (liminf) | BasicAnalysis.subsec.limsup | Uses `BasicAnalysis.ctxt.seq`. Put `i_N := inf { a_n \| n ≥ N }`; `(i_N)` is increasing and `liminf_{n→∞} a_n := sup_{N∈ℕ} i_N : ℝ̄`. Duality: `liminf a_n = − limsup (−a_n)`. Characterisation for `L : ℝ`: for every `ε > 0`, `a_n > L − ε` eventually and `a_n < L + ε` frequently. |
| BasicAnalysis.property.limsup_exists | property | limsup and liminf Always Exist in ℝ̄ | BasicAnalysis.def.limsup | For every `a : ℕ → ℝ`, `limsup a_n` and `liminf a_n` exist as elements of `ℝ̄` and satisfy `liminf a_n ≤ limsup a_n`. They are finite simultaneously with `a` being bounded: `limsup a_n < +∞` iff `a` is bounded above; `liminf a_n > −∞` iff `a` is bounded below. |
| BasicAnalysis.theorem.limsup_cluster | theorem | limsup/liminf as Extreme Cluster Points | BasicAnalysis.def.limsup | Uses `BasicAnalysis.ctxt.seq`. Let `C ⊆ ℝ̄` be the set of cluster points of `a` in the sense of `BasicAnalysis.def.cluster_point` (allowing `±∞`). Then `C ≠ ∅`, `limsup a_n = max C` and `liminf a_n = min C`; in particular both are themselves cluster points, i.e. attained by some subsequence. |
| BasicAnalysis.theorem.limsup_criterion | theorem | Convergence Criterion via limsup and liminf | BasicAnalysis.def.limsup | Uses `BasicAnalysis.ctxt.seq`. `a` converges to `L : ℝ` iff `limsup a_n = liminf a_n = L` (both finite). Likewise `a_n → +∞` iff `liminf a_n = +∞`, and `a_n → −∞` iff `limsup a_n = −∞`. |
| BasicAnalysis.proof.limsup_criterion | proof | Proof of the limsup/liminf Criterion | BasicAnalysis.theorem.limsup_criterion | (⇒) If `a_n → L`, then for `ε > 0` eventually `L − ε < a_n < L + ε`, so for large `N`, `L − ε ≤ i_N ≤ s_N ≤ L + ε`; letting `ε → 0` gives `liminf = limsup = L`. (⇐) If `liminf = limsup = L`, then for `ε > 0` there is `N` with `L − ε < i_N ≤ a_n ≤ s_N < L + ε` for all `n ≥ N`, i.e. `\|a_n − L\| < ε`. |
| BasicAnalysis.property.limsup_subadditive | property | Subadditivity of limsup | BasicAnalysis.def.limsup | For bounded `a, b : ℕ → ℝ`: `limsup (a_n + b_n) ≤ limsup a_n + limsup b_n` and `liminf a_n + liminf b_n ≤ liminf (a_n + b_n)`, with equality when one of the two sequences converges. Both inequalities can be strict: `a_n := (−1)ⁿ`, `b_n := (−1)^{n+1}` give `limsup(a_n+b_n) = 0 < 2`. For nonnegative sequences, `limsup (a_n b_n) ≤ (limsup a_n)(limsup b_n)`. |
| BasicAnalysis.remark.limsup_intuition | remark | Remark: Reading limsup as an Eventual Ceiling | BasicAnalysis.def.limsup | Intuition. `limsup a_n` is the smallest ceiling the sequence eventually stays under, up to any slack `ε`: it is exceeded only finitely often but approached infinitely often. Discarding the first `N` terms can only lower the ceiling, and the monotone limit of these ceilings is `limsup`. This is why `limsup` exists unconditionally while `lim` does not — one has replaced the two-sided pinning by a one-sided one. |
| BasicAnalysis.subsec.infinite | subsection | Infinitesimals and Infinities | BasicAnalysis.sec.seq | Null sequences, divergence to `±∞`, and their calculus. |
| BasicAnalysis.def.infinitesimal | definition | Infinitesimal (Null Sequence) | BasicAnalysis.subsec.infinite | Uses `BasicAnalysis.ctxt.seq`. `a` is an infinitesimal iff `a_n → 0`, i.e. `∀ ε > 0, ∃ N, ∀ n ≥ N, \|a_n\| < ε`. Standard reformulation of convergence: `a_n → L` iff `α_n := a_n − L` is an infinitesimal, so every convergent sequence factors as `a_n = L + α_n`. |
| BasicAnalysis.property.infinitesimal_algebra | property | Algebra of Infinitesimals | BasicAnalysis.def.infinitesimal | If `α` and `β` are infinitesimals then so are `α + β`, `α − β`, `c · α` for `c : ℝ`, and `α · β`. If `α` is an infinitesimal and `b` is bounded then `α_n · b_n` is an infinitesimal — note that `b` need not converge, e.g. `(1/n) · sin n → 0`. Quotients of infinitesimals are indeterminate. |
| BasicAnalysis.def.diverge_infinity | definition | Divergence to Infinity | BasicAnalysis.subsec.infinite | Uses `BasicAnalysis.ctxt.seq`. `a_n → +∞` iff `∀ M : ℝ, ∃ N : ℕ, ∀ n ≥ N, a_n > M`; `a_n → −∞` iff `∀ M : ℝ, ∃ N, ∀ n ≥ N, a_n < −M`; `a_n → ∞` (infinitely large in absolute value) iff `∀ M : ℝ, ∃ N, ∀ n ≥ N, \|a_n\| > M`. A sequence with `a_n → ±∞` is called an infinity; it is unbounded, hence divergent in the sense of `BasicAnalysis.def.limit`. |
| BasicAnalysis.property.infinity_reciprocal | property | Reciprocity between Infinitesimals and Infinities | BasicAnalysis.def.diverge_infinity | Uses `BasicAnalysis.ctxt.seq`. If `a_n ≠ 0` for all `n`, then `a_n → ∞` iff `1/a_n → 0`. Refinement with sign: if `a_n → 0` and `a_n > 0` eventually, then `1/a_n → +∞`. Also `a_n → +∞` and `b_n` bounded below imply `a_n + b_n → +∞`; `a_n → +∞` and `b_n ≥ c > 0` imply `a_n b_n → +∞`. |
| BasicAnalysis.counterexample.unbounded_not_infinite | counterexample | Counterexample: Unbounded ≠ Infinitely Large | BasicAnalysis.def.diverge_infinity | Let `a_n := n` for even `n` and `a_n := 0` for odd `n`. Then `a` is unbounded (`BasicAnalysis.def.bounded_seq` fails) but `a_n → ∞` fails: at `M = 1` no `N` works, because arbitrarily large odd indices give `\|a_n\| = 0`. Hence "unbounded" is strictly weaker than "infinitely large". |
| BasicAnalysis.remark.indeterminate_forms | remark | Remark: Indeterminate Forms | BasicAnalysis.subsec.infinite | Intuition. The symbolic patterns `∞ − ∞`, `0 · ∞`, `0/0`, `∞/∞`, `1^∞`, `0⁰`, `∞⁰` are indeterminate: knowing only the limits of the constituents does not determine the limit of the combination. With `a_n := n`, choosing `b_n := n`, `n + c`, or `n²` makes `a_n − b_n` tend to `0`, `−c`, or `−∞`; `(1 + 1/n)ⁿ → e` versus `1ⁿ → 1` shows `1^∞` is genuinely undetermined. Resolving such forms requires rate comparison (Stolz, ratio tests, or later L'Hôpital). |
| BasicAnalysis.subsec.standard_limits | subsection | Standard Limits and Techniques | BasicAnalysis.sec.seq | The reference limits and the two comparison tools (Stolz, ratio) used to evaluate them. |
| BasicAnalysis.theorem.limit_nth_root_n | theorem | `n^{1/n} → 1` | BasicAnalysis.subsec.standard_limits | `lim_{n→∞} n^{1/n} = 1`. Proof: write `n^{1/n} = 1 + h_n` with `h_n ≥ 0`; the binomial theorem gives `n = (1+h_n)ⁿ ≥ C(n,2) h_n² = n(n−1)h_n²/2`, so `0 ≤ h_n ≤ √(2/(n−1)) → 0`, and `BasicAnalysis.theorem.squeeze` applies. |
| BasicAnalysis.theorem.limit_nth_root_a | theorem | `a^{1/n} → 1` for `a > 0` | BasicAnalysis.subsec.standard_limits | For every `a : ℝ` with `a > 0`, `lim_{n→∞} a^{1/n} = 1`. Proof: for `a ≥ 1` Bernoulli gives `1 ≤ a^{1/n} ≤ 1 + (a−1)/n`; for `0 < a < 1` apply the previous case to `1/a` and use `BasicAnalysis.theorem.limit_div`. |
| BasicAnalysis.theorem.limit_poly_over_exp | theorem | Polynomial versus Exponential versus Factorial Growth | BasicAnalysis.subsec.standard_limits | For every fixed `α : ℝ` and `q : ℝ` with `q > 1`: `lim n^α / qⁿ = 0`; for every fixed `c : ℝ`: `lim cⁿ / n! = 0`; and `lim n! / nⁿ = 0`. Ordering of growth rates: `ln n ≺ n^α ≺ qⁿ ≺ n! ≺ nⁿ`, where `x ≺ y` means `x_n / y_n → 0`. |
| BasicAnalysis.theorem.limit_e | theorem | The Limit Defining `e` | BasicAnalysis.subsec.standard_limits | `lim_{n→∞} (1 + 1/n)ⁿ = e` (see `BasicAnalysis.example.euler_e`), and more generally `lim (1 + x/n)ⁿ = e^x` for every `x : ℝ`, and `lim (1 + 1/a_n)^{a_n} = e` for any real sequence with `a_n → ∞`. Equivalently `e = Σ_{k=0}^{∞} 1/k!`, with error bound `\|e − Σ_{k=0}^{n} 1/k!\| < 1/(n · n!)`. |
| BasicAnalysis.theorem.stolz | theorem | Stolz–Cesàro Theorem | BasicAnalysis.subsec.standard_limits | Uses `BasicAnalysis.ctxt.seq`. Let `b : ℕ → ℝ` be strictly increasing with `b_n → +∞` (the `∞/∞` case). If `lim (a_{n+1} − a_n)/(b_{n+1} − b_n) = L : ℝ̄` then `lim a_n / b_n = L`. The `0/0` case: if `a_n → 0`, `b_n → 0`, `b` strictly decreasing, and the same difference quotient tends to `L`, then `a_n/b_n → L`. The converse fails, so Stolz is a sufficient criterion only. |
| BasicAnalysis.corollary.cesaro_mean | corollary | Cesàro Mean | BasicAnalysis.theorem.stolz | Uses `BasicAnalysis.ctxt.seq`. If `a_n → L : ℝ̄` then the arithmetic means satisfy `(a_1 + … + a_n)/n → L`. If moreover `a_n > 0`, the geometric means satisfy `(a_1 ⋯ a_n)^{1/n} → L`, and if `a_{n+1}/a_n → L` then `a_n^{1/n} → L`. The converses fail: `a_n := (−1)ⁿ` has Cesàro mean tending to `0` while `a` diverges. |
| BasicAnalysis.problem.limit_practice | problem | Problems: Evaluating Standard Limits | BasicAnalysis.subsec.standard_limits | (1) Show `lim (√(n+1) − √n) = 0` by rationalising to `1/(√(n+1)+√n)`. (2) Show `lim (1 + 1/2 + … + 1/n)/n = 0` using `BasicAnalysis.corollary.cesaro_mean`. (3) Show `lim n^{1/n²} = 1`. (4) Show that `a_{n+1} := (a_n + c/a_n)/2` with `a_0 > 0`, `c > 0` is eventually decreasing and bounded below by `√c`, hence converges to `√c` by `BasicAnalysis.theorem.monotone_convergence`. (5) Compute `lim (Σ_{k=1}^{n} k^p)/n^{p+1}` for `p : ℕ` via `BasicAnalysis.theorem.stolz`. |
| BasicAnalysis.subsec.divergent | subsection | Divergent Sequences | BasicAnalysis.sec.seq | Classification of non-convergence and the standard tests for it. |
| BasicAnalysis.def.divergent | definition | Divergent Sequence | BasicAnalysis.subsec.divergent | Uses `BasicAnalysis.ctxt.seq`. `a` is divergent iff `¬ ∃ L : ℝ, a_n → L`, i.e. `∀ L : ℝ, ∃ ε > 0, ∀ N : ℕ, ∃ n ≥ N, \|a_n − L\| ≥ ε`. Divergence splits into: proper divergence to `±∞` (`BasicAnalysis.def.diverge_infinity`); oscillatory divergence with at least two distinct cluster points, in which case `liminf a_n < limsup a_n`; and unbounded oscillation. |
| BasicAnalysis.theorem.divergence_tests | theorem | Criteria for Divergence | BasicAnalysis.def.divergent | Uses `BasicAnalysis.ctxt.seq`. Each of the following implies that `a` diverges: (i) `a` is unbounded (contrapositive of `BasicAnalysis.theorem.conv_implies_bounded`); (ii) two subsequences converge to distinct limits (`BasicAnalysis.theorem.subseq_limit`); (iii) `liminf a_n ≠ limsup a_n` (`BasicAnalysis.theorem.limsup_criterion`); (iv) the Cauchy condition fails, i.e. `∃ ε₀ > 0, ∀ N, ∃ m,n ≥ N, \|a_m − a_n\| ≥ ε₀` (`BasicAnalysis.theorem.cauchy_criterion`). |
| BasicAnalysis.example.divergent_sin | example | Example: `(sin n)` Diverges | BasicAnalysis.theorem.divergence_tests | The sequence `a_n := sin n` is bounded by `1` but divergent: by the equidistribution of `{n/(2π)}` mod 1 (Weyl), the set of cluster points of `(sin n)` is the whole interval `[−1, 1]`, so `liminf = −1 < 1 = limsup` and criterion (iii) of `BasicAnalysis.theorem.divergence_tests` applies. An elementary argument uses the identity `sin(n+1) + sin(n−1) = 2 sin n cos 1` to derive a contradiction from `sin n → L`. |
| BasicAnalysis.example.divergent_harmonic | example | Example: The Harmonic Partial Sums Diverge to `+∞` | BasicAnalysis.theorem.divergence_tests | With `H_n := Σ_{k=1}^{n} 1/k`, blocking gives `H_{2^m} ≥ 1 + m/2`, so `H` is unbounded; since `H` is increasing, `BasicAnalysis.theorem.monotone_convergence` forces `H_n → +∞`. Cf. `BasicAnalysis.counterexample.cauchy_pointwise`. |
| BasicAnalysis.remark.divergence_vs_infinity | remark | Remark: Divergence Is Not Just "Going to Infinity" | BasicAnalysis.def.divergent | Intuition. In everyday usage "divergent" suggests blow-up, but the formal negation of `BasicAnalysis.def.limit` is merely "no real number is approached". Bounded oscillation (`(−1)ⁿ`), unbounded oscillation (`n·(−1)ⁿ`), and proper divergence (`n`) are all divergent, yet only the last two are unbounded and only the last one is an infinity. Writing `lim a_n = +∞` is a notational convenience for `BasicAnalysis.def.diverge_infinity`, not an assertion that a limit exists in ℝ. |
| BasicAnalysis.problem.divergence_practice | problem | Problems: Divergence and Cluster Sets | BasicAnalysis.subsec.divergent | (1) Determine the cluster set of `a_n := (−1)ⁿ (1 + 1/n)` and its `liminf`/`limsup`. (2) Construct a sequence whose set of cluster points is exactly ℚ ∩ [0,1] (enumerate the rationals and repeat each with increasing multiplicity). (3) Show that a sequence is convergent iff every subsequence has a convergent further subsequence with a common limit. (4) Show that if `a_n → +∞` then `liminf a_n = limsup a_n = +∞`, and that the sequence has no bounded subsequence. |
