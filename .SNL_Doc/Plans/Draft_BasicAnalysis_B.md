# Draft — BasicAnalysis Part B: Series, Function Limits & Continuity

**Scope.** This draft is the Phase-1 blueprint for two blocks of the `BasicAnalysis` library:

1. **Series** — series and partial sums; convergence / divergence; absolute vs. conditional convergence; Cauchy convergence criterion; rearrangement (Riemann rearrangement theorem, invariance under rearrangement for absolutely convergent series); tests for positive-term series (comparison, ratio, root, integral, Raabe); tests for arbitrary-term series (Leibniz, Abel, Dirichlet).
2. **Function limits & continuity** — ε–δ definition of a function limit; one-sided limits; Heine's theorem (sequential characterization); arithmetic of limits; continuity; algebraic operations and composition of continuous functions; classification of discontinuities; properties of continuous functions on a closed interval (boundedness, extreme value theorem, intermediate value theorem, uniform continuity and Cantor's theorem).

All entry ids use the `BasicAnalysis` domain prefix. `parent` refers to the parent node id inside the library graph (`section` rows have empty parent). Content drafts are in natural English; formal SNL will be produced in Phase 3.

| id | kind | title | parent | content |
|----|------|-------|--------|---------|
| BasicAnalysis.sec.series | section | Series |  | Top-level section grouping infinite series over the real line: definitions, convergence notions, rearrangement behaviour, and tests for both positive-term and arbitrary-term series. |
| BasicAnalysis.ctxt.series | context | Ambient sequence | BasicAnalysis.sec.series | Fix a real sequence `a : ℕ → ℝ`. Throughout the Series section, `a_n` denotes the n-th term and `S_n = a_1 + a_2 + … + a_n` denotes the n-th partial sum. |
| BasicAnalysis.subsec.seriesBasic | subsection | Series and convergence | BasicAnalysis.sec.series | Basic vocabulary: what a series is, what its partial sums are, and what convergence, divergence, and sum mean. |
| BasicAnalysis.def.series | definition | Infinite series | BasicAnalysis.subsec.seriesBasic | Given a sequence `a : ℕ → ℝ`, the *infinite series* associated with `a` is the formal expression `∑_{n=1}^{∞} a_n`, together with its sequence of partial sums `S_n := ∑_{k=1}^{n} a_k`. |
| BasicAnalysis.def.partialSum | definition | Partial sum | BasicAnalysis.subsec.seriesBasic | The n-th partial sum of the series `∑ a_n` is `S_n := a_1 + a_2 + … + a_n`, i.e. `S_n = ∑_{k=1}^{n} a_k`. |
| BasicAnalysis.def.convergentSeries | definition | Convergent series and sum | BasicAnalysis.subsec.seriesBasic | The series `∑ a_n` *converges* iff the sequence `(S_n)` of its partial sums converges in ℝ. In that case its *sum* is `S := lim_{n→∞} S_n`, and one writes `∑_{n=1}^{∞} a_n = S`. |
| BasicAnalysis.def.divergentSeries | definition | Divergent series | BasicAnalysis.subsec.seriesBasic | The series `∑ a_n` *diverges* iff the sequence `(S_n)` of its partial sums does not converge in ℝ (either has no limit, or diverges to ±∞). |
| BasicAnalysis.def.tail | definition | Tail of a series | BasicAnalysis.subsec.seriesBasic | For each `N ∈ ℕ`, the *N-th tail* of `∑ a_n` is the series `∑_{n=N+1}^{∞} a_n`. The original series converges iff some (equivalently, every) tail converges, and in that case `∑_{n=1}^{∞} a_n = S_N + ∑_{n=N+1}^{∞} a_n`. |
| BasicAnalysis.thm.necessaryTermToZero | theorem | Necessary condition: term tends to zero | BasicAnalysis.subsec.seriesBasic | If `∑ a_n` converges, then `lim_{n→∞} a_n = 0`. |
| BasicAnalysis.proof.necessaryTermToZero | proof | Proof of necessary condition | BasicAnalysis.thm.necessaryTermToZero | Since `a_n = S_n − S_{n−1}` and `(S_n)` converges to some `S`, we get `a_n → S − S = 0`. |
| BasicAnalysis.remark.termToZeroNotSufficient | remark | The converse fails | BasicAnalysis.thm.necessaryTermToZero | `a_n → 0` does not imply convergence: the harmonic series `∑ 1/n` diverges although `1/n → 0`. |
| BasicAnalysis.thm.linearityOfSeries | theorem | Linearity of convergent series | BasicAnalysis.subsec.seriesBasic | If `∑ a_n = A` and `∑ b_n = B` converge and `α, β ∈ ℝ`, then `∑ (α a_n + β b_n)` converges and equals `αA + βB`. |
| BasicAnalysis.thm.cauchyCriterionSeries | theorem | Cauchy criterion for series | BasicAnalysis.subsec.seriesBasic | The series `∑ a_n` converges iff for every `ε > 0` there exists `N ∈ ℕ` such that for all integers `n > m ≥ N`, `|a_{m+1} + a_{m+2} + … + a_n| < ε`. |
| BasicAnalysis.proof.cauchyCriterionSeries | proof | Proof via Cauchy criterion for sequences | BasicAnalysis.thm.cauchyCriterionSeries | `∑ a_n` converges iff `(S_n)` is Cauchy. Since `S_n − S_m = a_{m+1} + … + a_n` for `n > m`, the sequence Cauchy criterion translates verbatim. |
| BasicAnalysis.example.geometricSeries | example | Geometric series | BasicAnalysis.subsec.seriesBasic | For `q ∈ ℝ`, `∑_{n=0}^{∞} q^n` converges iff `|q| < 1`, in which case its sum is `1/(1−q)`. If `|q| ≥ 1` it diverges. |
| BasicAnalysis.example.harmonicSeries | example | Harmonic series diverges | BasicAnalysis.subsec.seriesBasic | The series `∑_{n=1}^{∞} 1/n` diverges. A block estimate `1/(2^k+1) + … + 1/2^{k+1} ≥ 1/2` shows the partial sums are unbounded. |
| BasicAnalysis.example.pSeries | example | p-series | BasicAnalysis.subsec.seriesBasic | For `p ∈ ℝ`, the series `∑_{n=1}^{∞} 1/n^p` converges iff `p > 1` and diverges iff `p ≤ 1`. |
| BasicAnalysis.def.absConvergent | definition | Absolutely convergent series | BasicAnalysis.subsec.seriesBasic | The series `∑ a_n` is *absolutely convergent* iff `∑ |a_n|` converges. |
| BasicAnalysis.def.condConvergent | definition | Conditionally convergent series | BasicAnalysis.subsec.seriesBasic | The series `∑ a_n` is *conditionally convergent* iff `∑ a_n` converges but `∑ |a_n|` diverges. |
| BasicAnalysis.thm.absImpliesConv | theorem | Absolute convergence implies convergence | BasicAnalysis.subsec.seriesBasic | If `∑ |a_n|` converges, then `∑ a_n` converges, and `|∑_{n=1}^{∞} a_n| ≤ ∑_{n=1}^{∞} |a_n|`. |
| BasicAnalysis.proof.absImpliesConv | proof | Proof via Cauchy criterion | BasicAnalysis.thm.absImpliesConv | By the Cauchy criterion applied to `∑|a_n|`, for every ε > 0 there is `N` with `∑_{k=m+1}^{n} |a_k| < ε` for `n > m ≥ N`. The triangle inequality `|∑_{k=m+1}^{n} a_k| ≤ ∑_{k=m+1}^{n} |a_k| < ε` gives the Cauchy condition for `∑ a_n`, hence convergence; passing to limits in `|S_n| ≤ ∑_{k=1}^{n}|a_k|` yields the bound. |
| BasicAnalysis.example.altHarmonicCond | example | Alternating harmonic series is conditionally convergent | BasicAnalysis.def.condConvergent | The series `∑_{n=1}^{∞} (−1)^{n−1}/n` converges (by Leibniz) but `∑ 1/n` diverges; hence it is conditionally, not absolutely, convergent. |
| BasicAnalysis.subsec.rearrangement | subsection | Rearrangement of series | BasicAnalysis.sec.series | Behaviour of a series under a reindexing bijection `σ : ℕ → ℕ`: invariance for absolutely convergent series, wild sensitivity for conditionally convergent series. |
| BasicAnalysis.def.rearrangement | definition | Rearrangement | BasicAnalysis.subsec.rearrangement | Given a series `∑ a_n` and a bijection `σ : ℕ → ℕ`, the *rearrangement of `∑ a_n` by `σ`* is the series `∑ a_{σ(n)}`. |
| BasicAnalysis.thm.absRearrangementInvariant | theorem | Rearrangement invariance for absolute convergence | BasicAnalysis.subsec.rearrangement | If `∑ a_n` converges absolutely with sum `S`, then for every bijection `σ : ℕ → ℕ` the rearranged series `∑ a_{σ(n)}` also converges absolutely and has sum `S`. |
| BasicAnalysis.proof.absRearrangementInvariant | proof | Proof by tail control | BasicAnalysis.thm.absRearrangementInvariant | Given ε > 0, pick `N` with `∑_{k>N} |a_k| < ε/2`. Choose `M` large enough that `{1,…,N} ⊆ σ({1,…,M})`. For `n ≥ M`, the partial sum `∑_{k=1}^{n} a_{σ(k)}` differs from `∑_{k=1}^{N} a_k` only by terms with indices > N, whose absolute values sum to less than ε/2. Combined with `|S − ∑_{k=1}^N a_k| < ε/2`, the rearranged partial sum stays within ε of S. Absolute convergence of the rearrangement follows by the same argument applied to `|a_n|`. |
| BasicAnalysis.thm.riemannRearrangement | theorem | Riemann rearrangement theorem | BasicAnalysis.subsec.rearrangement | Let `∑ a_n` be a conditionally convergent series of real numbers. Then for every `α ∈ ℝ ∪ {−∞, +∞}` there exists a bijection `σ : ℕ → ℕ` such that `∑_{n=1}^{∞} a_{σ(n)} = α`. Moreover, there also exist bijections making the rearranged partial sums oscillate without limit. |
| BasicAnalysis.proof.riemannRearrangement | proof | Proof by greedy interleaving of positive and negative parts | BasicAnalysis.thm.riemannRearrangement | Split `a_n` into positive and negative parts `p_n = max(a_n,0)`, `q_n = max(−a_n,0)`. Conditional convergence forces `∑ p_n = +∞` and `∑ q_n = +∞`, while `p_n, q_n → 0`. To hit target `α ∈ ℝ`: greedily append positive terms until the partial sum first exceeds α, then negative terms until it first drops below α, and repeat. Because `p_n, q_n → 0`, the overshoot at each switch tends to 0, so the partial sums converge to α. Targets ±∞ and oscillation follow by analogous greedy schemes. |
| BasicAnalysis.remark.rearrangementSensitivity | remark | Conditional convergence is fragile | BasicAnalysis.thm.riemannRearrangement | Riemann's theorem shows that for conditionally convergent real series the notion of "sum" is tied to the given order of summation; only absolute convergence delivers a truly order-independent value. |
| BasicAnalysis.subsec.positiveTests | subsection | Convergence tests for positive-term series | BasicAnalysis.sec.series | Tests specialized to series `∑ a_n` with `a_n ≥ 0`: comparison, ratio, root, integral, Raabe. In this subsection assume `a_n ≥ 0` for all n unless stated otherwise. |
| BasicAnalysis.ctxt.positiveSeries | context | Positive-term series context | BasicAnalysis.subsec.positiveTests | Fix sequences `a, b : ℕ → ℝ` with `a_n ≥ 0` and `b_n ≥ 0`. `S_n = ∑_{k=1}^n a_k` is the partial sum of `∑ a_n`. |
| BasicAnalysis.thm.positiveMonotoneCriterion | theorem | Monotone criterion for positive series | BasicAnalysis.subsec.positiveTests | If `a_n ≥ 0` for all n, then `∑ a_n` converges iff the sequence of partial sums `(S_n)` is bounded above; otherwise `S_n → +∞`. |
| BasicAnalysis.proof.positiveMonotoneCriterion | proof | Proof | BasicAnalysis.thm.positiveMonotoneCriterion | Since `a_n ≥ 0`, `(S_n)` is monotone increasing. A monotone increasing real sequence converges iff it is bounded above, and otherwise tends to `+∞`. |
| BasicAnalysis.thm.comparisonTest | theorem | Comparison test | BasicAnalysis.subsec.positiveTests | Suppose there exists `N ∈ ℕ` such that `0 ≤ a_n ≤ b_n` for all `n ≥ N`. Then: (i) if `∑ b_n` converges, so does `∑ a_n`; (ii) if `∑ a_n` diverges, so does `∑ b_n`. |
| BasicAnalysis.proof.comparisonTest | proof | Proof by monotone criterion | BasicAnalysis.thm.comparisonTest | The tail partial sums of `∑ a_n` are bounded above by the corresponding tail partial sums of `∑ b_n`. If `∑ b_n` converges, its tail partial sums are bounded, hence so are those of `∑ a_n`, and by the monotone criterion `∑ a_n` converges. Contrapositive gives (ii). |
| BasicAnalysis.corollary.limitComparisonTest | corollary | Limit form of the comparison test | BasicAnalysis.thm.comparisonTest | Suppose `a_n, b_n > 0` and `L := lim_{n→∞} a_n/b_n` exists in `[0, +∞]`. Then: (i) if `0 < L < +∞`, `∑ a_n` and `∑ b_n` converge or diverge together; (ii) if `L = 0` and `∑ b_n` converges, then `∑ a_n` converges; (iii) if `L = +∞` and `∑ b_n` diverges, then `∑ a_n` diverges. |
| BasicAnalysis.thm.ratioTest | theorem | d'Alembert (ratio) test | BasicAnalysis.subsec.positiveTests | Let `a_n > 0` and suppose `L := lim_{n→∞} a_{n+1}/a_n` exists in `[0, +∞]`. Then: if `L < 1` the series `∑ a_n` converges; if `L > 1` (including `+∞`) it diverges; if `L = 1` the test is inconclusive. |
| BasicAnalysis.proof.ratioTest | proof | Proof by geometric domination | BasicAnalysis.thm.ratioTest | If `L < 1`, choose `q` with `L < q < 1`; then for `n ≥ N` we have `a_{n+1}/a_n < q`, hence `a_n ≤ a_N q^{n−N}`. Comparison with the geometric series `∑ q^n` gives convergence. If `L > 1`, for large n we have `a_{n+1} > a_n`, so `a_n` does not tend to 0 and the series diverges. |
| BasicAnalysis.thm.rootTest | theorem | Cauchy (root) test | BasicAnalysis.subsec.positiveTests | Let `a_n ≥ 0` and set `L := limsup_{n→∞} a_n^{1/n}`. Then: if `L < 1`, `∑ a_n` converges; if `L > 1`, `∑ a_n` diverges; if `L = 1`, the test is inconclusive. |
| BasicAnalysis.proof.rootTest | proof | Proof by geometric domination | BasicAnalysis.thm.rootTest | If `L < 1`, choose `q` with `L < q < 1`; then for all n large enough, `a_n^{1/n} < q`, i.e. `a_n < q^n`; compare with `∑ q^n`. If `L > 1`, infinitely many `a_n > 1`, so `a_n ↛ 0` and the series diverges. |
| BasicAnalysis.remark.rootVsRatio | remark | Root test dominates ratio test | BasicAnalysis.thm.rootTest | For positive sequences, `liminf a_{n+1}/a_n ≤ liminf a_n^{1/n} ≤ limsup a_n^{1/n} ≤ limsup a_{n+1}/a_n`. Hence whenever the ratio test decides, the root test decides identically; the root test can succeed where the ratio limit does not exist. |
| BasicAnalysis.thm.integralTest | theorem | Cauchy integral test | BasicAnalysis.subsec.positiveTests | Let `f : [1, +∞) → [0, +∞)` be nonincreasing. Then the series `∑_{n=1}^{∞} f(n)` and the improper integral `∫_{1}^{∞} f(x) dx` converge or diverge together. |
| BasicAnalysis.proof.integralTest | proof | Proof by staircase estimates | BasicAnalysis.thm.integralTest | Since `f` is nonincreasing, for each integer `k ≥ 1`, `f(k+1) ≤ ∫_{k}^{k+1} f(x) dx ≤ f(k)`. Summing from `k = 1` to `n` gives `∑_{k=2}^{n+1} f(k) ≤ ∫_{1}^{n+1} f(x) dx ≤ ∑_{k=1}^{n} f(k)`. Both sides are monotone, so boundedness of the integral over `[1, ∞)` is equivalent to boundedness of the partial sums. |
| BasicAnalysis.example.pSeriesViaIntegral | example | p-series revisited via integral test | BasicAnalysis.thm.integralTest | Applying the integral test to `f(x) = 1/x^p` on `[1, ∞)` recovers: `∑ 1/n^p` converges iff `p > 1`. |
| BasicAnalysis.thm.raabeTest | theorem | Raabe's test | BasicAnalysis.subsec.positiveTests | Let `a_n > 0` and suppose `R := lim_{n→∞} n · (a_n/a_{n+1} − 1)` exists in `[−∞, +∞]`. Then: if `R > 1`, `∑ a_n` converges; if `R < 1`, `∑ a_n` diverges; if `R = 1` the test is inconclusive. |
| BasicAnalysis.proof.raabeTest | proof | Proof by comparison with p-series | BasicAnalysis.thm.raabeTest | Suppose `R > 1`; pick `r` with `1 < r < R`. For large n, `n(a_n/a_{n+1} − 1) ≥ r`, i.e. `a_{n+1}/a_n ≤ 1 − r/n + o(1/n)`. Compare with the terms of a convergent p-series `1/n^r`: iterating the ratio bound shows `a_n = O(1/n^r)`, and comparison gives convergence. If `R < 1`, analogous inequalities give `a_n ≥ c/n` eventually, and comparison with the harmonic series gives divergence. |
| BasicAnalysis.remark.raabeUse | remark | When to use Raabe | BasicAnalysis.thm.raabeTest | Raabe's test is designed for the borderline case where the ratio test gives `L = 1`; it detects the next-order behaviour `a_{n+1}/a_n = 1 − R/n + o(1/n)`. |
| BasicAnalysis.subsec.arbitraryTests | subsection | Convergence tests for arbitrary-term series | BasicAnalysis.sec.series | Tests that do not require the terms to be nonnegative: Leibniz's alternating-series test, Abel's test, and Dirichlet's test, all built on Abel's partial-summation formula. |
| BasicAnalysis.lemma.abelSummation | lemma | Abel summation by parts | BasicAnalysis.subsec.arbitraryTests | Let `(a_n), (b_n) ⊆ ℝ` and set `B_n := b_1 + b_2 + … + b_n` (with `B_0 = 0`). Then for all `1 ≤ m ≤ n`, `∑_{k=m}^{n} a_k b_k = a_n B_n − a_m B_{m−1} − ∑_{k=m}^{n−1} (a_{k+1} − a_k) B_k`. |
| BasicAnalysis.proof.abelSummation | proof | Proof by telescoping | BasicAnalysis.lemma.abelSummation | Write `b_k = B_k − B_{k−1}`, substitute into the left-hand side and reindex the resulting telescoping sum: `∑_{k=m}^{n} a_k (B_k − B_{k−1}) = a_n B_n − a_m B_{m−1} + ∑_{k=m}^{n−1} B_k (a_k − a_{k+1})`. |
| BasicAnalysis.thm.leibnizTest | theorem | Leibniz alternating-series test | BasicAnalysis.subsec.arbitraryTests | If `(b_n)` is a monotone nonincreasing sequence of nonnegative reals with `b_n → 0`, then the alternating series `∑_{n=1}^{∞} (−1)^{n−1} b_n` converges. Moreover, if `S` is its sum and `S_n` its n-th partial sum, then `|S − S_n| ≤ b_{n+1}`. |
| BasicAnalysis.proof.leibnizTest | proof | Proof via nested partial sums | BasicAnalysis.thm.leibnizTest | The subsequences `S_{2k}` and `S_{2k+1}` are respectively increasing and decreasing, with `S_{2k} ≤ S_{2k+1}` and `S_{2k+1} − S_{2k} = b_{2k+1} → 0`. By the nested-interval principle they converge to a common limit `S`. The error bound follows because `S` lies between consecutive partial sums. |
| BasicAnalysis.thm.dirichletTest | theorem | Dirichlet's test | BasicAnalysis.subsec.arbitraryTests | Let `(a_n)` be a monotone sequence with `a_n → 0`, and let `(b_n)` have bounded partial sums, i.e. there exists `M > 0` with `|b_1 + b_2 + … + b_n| ≤ M` for all n. Then `∑ a_n b_n` converges. |
| BasicAnalysis.proof.dirichletTest | proof | Proof via Abel summation and Cauchy criterion | BasicAnalysis.thm.dirichletTest | Apply Abel summation to `∑_{k=m}^{n} a_k b_k`. Boundedness of `(B_n)` by `M` and monotonicity of `(a_n)` give `|∑_{k=m}^{n} a_k b_k| ≤ M(|a_n| + |a_m| + ∑ |a_{k+1} − a_k|) ≤ 3M · |a_m|` (via telescoping the monotone increments). Since `a_m → 0`, the Cauchy criterion applies. |
| BasicAnalysis.thm.abelTest | theorem | Abel's test | BasicAnalysis.subsec.arbitraryTests | If `(a_n)` is a monotone bounded sequence and `∑ b_n` converges, then `∑ a_n b_n` converges. |
| BasicAnalysis.proof.abelTest | proof | Proof by reduction to Dirichlet | BasicAnalysis.thm.abelTest | By monotone convergence `a_n → A` for some `A ∈ ℝ`. Write `a_n = (a_n − A) + A`; then `(a_n − A)` is monotone with limit 0, and `∑ b_n` has bounded partial sums (being convergent). Dirichlet's test gives convergence of `∑ (a_n − A) b_n`, and `A ∑ b_n` converges by assumption; their sum is `∑ a_n b_n`. |
| BasicAnalysis.example.dirichletTrig | example | Dirichlet test for `∑ sin(nx)/n` | BasicAnalysis.thm.dirichletTest | For every `x ∈ ℝ`, the series `∑_{n=1}^{∞} sin(nx)/n` converges. Indeed, `a_n = 1/n ↘ 0` monotonically, and the partial sums `∑_{k=1}^{n} sin(kx)` are bounded by `1/|sin(x/2)|` (when `x ∉ 2πℤ`), so Dirichlet's test applies; if `x ∈ 2πℤ` every term is 0. |
| BasicAnalysis.sec.functionLimits | section | Function limits and continuity |  | Top-level section on limits of real-valued functions and continuity: ε–δ definitions, sequential (Heine) characterization, algebraic operations, classification of discontinuities, and the classical properties of continuous functions on a closed interval. |
| BasicAnalysis.ctxt.functionAmbient | context | Ambient function and point | BasicAnalysis.sec.functionLimits | Fix an interval or set `I ⊆ ℝ`, a function `f : I → ℝ`, and a point `x_0 ∈ ℝ` that is either a point of `I` or a limit point of `I`. Symbols `δ, ε > 0` denote positive reals. When two functions are considered simultaneously we write `g : I → ℝ`. |
| BasicAnalysis.subsec.funcLimits | subsection | Function limits and Heine's theorem | BasicAnalysis.sec.functionLimits | Definition of `lim_{x→x_0} f(x) = L` in ε–δ style, one-sided variants, and the equivalence with sequential limits (Heine's theorem). |
| BasicAnalysis.def.funcLimit | definition | Limit of a function at a point | BasicAnalysis.subsec.funcLimits | Let `x_0` be a limit point of `I`. We say `lim_{x→x_0} f(x) = L` iff for every `ε > 0` there exists `δ > 0` such that for every `x ∈ I` with `0 < |x − x_0| < δ`, `|f(x) − L| < ε`. |
| BasicAnalysis.def.funcLimitLeft | definition | Left-hand limit | BasicAnalysis.subsec.funcLimits | We say `lim_{x→x_0^{−}} f(x) = L` iff for every `ε > 0` there exists `δ > 0` such that for every `x ∈ I` with `x_0 − δ < x < x_0`, `|f(x) − L| < ε`. |
| BasicAnalysis.def.funcLimitRight | definition | Right-hand limit | BasicAnalysis.subsec.funcLimits | We say `lim_{x→x_0^{+}} f(x) = L` iff for every `ε > 0` there exists `δ > 0` such that for every `x ∈ I` with `x_0 < x < x_0 + δ`, `|f(x) − L| < ε`. |
| BasicAnalysis.thm.limitIffOneSided | theorem | Two-sided limit via one-sided limits | BasicAnalysis.subsec.funcLimits | Let `x_0` be an interior limit point of `I`. Then `lim_{x→x_0} f(x) = L` iff both one-sided limits `lim_{x→x_0^{−}} f(x)` and `lim_{x→x_0^{+}} f(x)` exist and equal `L`. |
| BasicAnalysis.thm.limitUnique | theorem | Uniqueness of the limit | BasicAnalysis.subsec.funcLimits | If `lim_{x→x_0} f(x) = L_1` and `lim_{x→x_0} f(x) = L_2`, then `L_1 = L_2`. |
| BasicAnalysis.proof.limitUnique | proof | Proof by ε/2 argument | BasicAnalysis.thm.limitUnique | Assume `L_1 ≠ L_2`; set `ε = |L_1 − L_2|/3 > 0`. Pick `δ_1, δ_2 > 0` witnessing the ε-definitions. For any `x ∈ I` with `0 < |x − x_0| < min(δ_1, δ_2)` (which exists since `x_0` is a limit point), `|L_1 − L_2| ≤ |f(x) − L_1| + |f(x) − L_2| < 2ε = 2|L_1 − L_2|/3`, contradicting `|L_1 − L_2| > 0`. |
| BasicAnalysis.thm.heine | theorem | Heine's theorem | BasicAnalysis.subsec.funcLimits | `lim_{x→x_0} f(x) = L` iff for every sequence `(x_n) ⊆ I \ {x_0}` with `x_n → x_0`, the sequence `(f(x_n))` converges to `L`. |
| BasicAnalysis.proof.heine | proof | Proof (both directions) | BasicAnalysis.thm.heine | (⇒) Given ε, pick δ from the ε–δ definition. Since `x_n → x_0` there exists `N` with `0 < |x_n − x_0| < δ` for `n ≥ N`, whence `|f(x_n) − L| < ε`. (⇐) Contrapositive: if the ε–δ condition fails, there exists `ε_0 > 0` such that for every `n ∈ ℕ` some `x_n ∈ I` satisfies `0 < |x_n − x_0| < 1/n` yet `|f(x_n) − L| ≥ ε_0`. Then `x_n → x_0` but `f(x_n) ↛ L`, contradicting the sequential hypothesis. |
| BasicAnalysis.corollary.heineDivergence | corollary | Divergence via sequences | BasicAnalysis.thm.heine | If there exist two sequences `(x_n), (y_n) ⊆ I \ {x_0}` both tending to `x_0` such that `lim f(x_n) ≠ lim f(y_n)` (or one of the limits fails to exist), then `lim_{x→x_0} f(x)` does not exist. |
| BasicAnalysis.thm.limitArithmetic | theorem | Arithmetic of function limits | BasicAnalysis.subsec.funcLimits | Suppose `lim_{x→x_0} f(x) = A` and `lim_{x→x_0} g(x) = B`. Then: (i) `lim_{x→x_0} (f(x) ± g(x)) = A ± B`; (ii) `lim_{x→x_0} (f(x) g(x)) = A B`; (iii) if `B ≠ 0`, `lim_{x→x_0} f(x)/g(x) = A/B`. |
| BasicAnalysis.proof.limitArithmetic | proof | Proof via Heine | BasicAnalysis.thm.limitArithmetic | By Heine's theorem, function limits reduce to sequential limits, and the arithmetic of sequential limits transfers each identity termwise. For (iii) one additionally uses that `g(x_n) → B ≠ 0`, so `g(x_n) ≠ 0` for large n, allowing the quotient to be formed. |
| BasicAnalysis.thm.limitSqueeze | theorem | Squeeze theorem for functions | BasicAnalysis.subsec.funcLimits | Suppose `h, f, g : I → ℝ` satisfy `h(x) ≤ f(x) ≤ g(x)` in a punctured neighbourhood of `x_0`, and `lim_{x→x_0} h(x) = lim_{x→x_0} g(x) = L`. Then `lim_{x→x_0} f(x) = L`. |
| BasicAnalysis.subsec.continuity | subsection | Continuity | BasicAnalysis.sec.functionLimits | Continuity at a point, on a set, and its interaction with algebraic operations and composition. |
| BasicAnalysis.def.continuousAtPoint | definition | Continuous at a point | BasicAnalysis.subsec.continuity | Let `x_0 ∈ I`. The function `f : I → ℝ` is *continuous at `x_0`* iff for every `ε > 0` there exists `δ > 0` such that for every `x ∈ I` with `|x − x_0| < δ`, `|f(x) − f(x_0)| < ε`. Equivalently, if `x_0` is a limit point of `I`, `lim_{x→x_0} f(x) = f(x_0)`. |
| BasicAnalysis.def.continuousOnSet | definition | Continuous on a set | BasicAnalysis.subsec.continuity | The function `f : I → ℝ` is *continuous on `I`* iff `f` is continuous at every point `x_0 ∈ I`. |
| BasicAnalysis.def.leftRightContinuous | definition | Left/right continuity | BasicAnalysis.subsec.continuity | `f` is *left-continuous at `x_0`* iff `lim_{x→x_0^{−}} f(x) = f(x_0)`; it is *right-continuous at `x_0`* iff `lim_{x→x_0^{+}} f(x) = f(x_0)`. `f` is continuous at an interior point `x_0` iff it is both left- and right-continuous there. |
| BasicAnalysis.thm.continuityArithmetic | theorem | Arithmetic of continuous functions | BasicAnalysis.subsec.continuity | If `f, g : I → ℝ` are continuous at `x_0 ∈ I`, then `f ± g` and `f · g` are continuous at `x_0`. If moreover `g(x_0) ≠ 0`, then `f/g` (defined on `{x ∈ I : g(x) ≠ 0}`) is continuous at `x_0`. |
| BasicAnalysis.thm.compositionContinuous | theorem | Composition of continuous functions | BasicAnalysis.subsec.continuity | Let `f : I → J ⊆ ℝ` be continuous at `x_0 ∈ I` and let `g : J → ℝ` be continuous at `y_0 = f(x_0) ∈ J`. Then `g ∘ f : I → ℝ` is continuous at `x_0`. |
| BasicAnalysis.proof.compositionContinuous | proof | Proof by chaining ε–δ | BasicAnalysis.thm.compositionContinuous | Given ε > 0, continuity of `g` at `y_0` yields `η > 0` with `|y − y_0| < η ⇒ |g(y) − g(y_0)| < ε`. Continuity of `f` at `x_0` yields `δ > 0` with `|x − x_0| < δ ⇒ |f(x) − y_0| < η`. Chaining, `|x − x_0| < δ ⇒ |g(f(x)) − g(f(x_0))| < ε`. |
| BasicAnalysis.example.polynomialContinuous | example | Polynomials and rational functions are continuous | BasicAnalysis.thm.continuityArithmetic | Constants and the identity `x ↦ x` are continuous on ℝ; by arithmetic of continuous functions every polynomial is continuous on ℝ, and every rational function is continuous on its natural domain (where the denominator is nonzero). |
| BasicAnalysis.subsec.discontinuities | subsection | Classification of discontinuities | BasicAnalysis.sec.functionLimits | Points where `f` fails to be continuous are classified into removable, jump, and essential discontinuities according to the behaviour of the one-sided limits. |
| BasicAnalysis.def.discontinuity | definition | Discontinuity point | BasicAnalysis.subsec.discontinuities | A point `x_0 ∈ I` is a *discontinuity point* of `f : I → ℝ` iff `f` is not continuous at `x_0`. |
| BasicAnalysis.def.removableDiscontinuity | definition | Removable discontinuity (first-kind, matching one-sided limits) | BasicAnalysis.subsec.discontinuities | An interior point `x_0` is a *removable discontinuity* of `f` iff both one-sided limits `lim_{x→x_0^{−}} f(x)` and `lim_{x→x_0^{+}} f(x)` exist, are finite, and are equal, but their common value differs from `f(x_0)` (or `f(x_0)` is undefined). Redefining `f(x_0)` to equal this common value makes `f` continuous at `x_0`. |
| BasicAnalysis.def.jumpDiscontinuity | definition | Jump discontinuity (first-kind, unequal one-sided limits) | BasicAnalysis.subsec.discontinuities | An interior point `x_0` is a *jump discontinuity* of `f` iff both one-sided limits `lim_{x→x_0^{−}} f(x)` and `lim_{x→x_0^{+}} f(x)` exist and are finite, but are not equal. The *jump* is `lim_{x→x_0^{+}} f(x) − lim_{x→x_0^{−}} f(x)`. |
| BasicAnalysis.def.essentialDiscontinuity | definition | Essential (second-kind) discontinuity | BasicAnalysis.subsec.discontinuities | An interior point `x_0` is an *essential (second-kind) discontinuity* of `f` iff at least one of the one-sided limits `lim_{x→x_0^{−}} f(x)`, `lim_{x→x_0^{+}} f(x)` fails to exist as a finite real number (either does not exist or is infinite). |
| BasicAnalysis.example.removableExample | example | Removable discontinuity: `sin(x)/x` extended to 0 | BasicAnalysis.def.removableDiscontinuity | Define `f(x) = sin(x)/x` for `x ≠ 0` and `f(0) = 0`. Since `lim_{x→0} sin(x)/x = 1 ≠ 0 = f(0)`, the origin is a removable discontinuity; setting `f(0) := 1` makes `f` continuous on ℝ. |
| BasicAnalysis.example.jumpExample | example | Jump discontinuity: `sgn(x)` at 0 | BasicAnalysis.def.jumpDiscontinuity | The sign function `sgn(x) = 1, 0, −1` for `x > 0, x = 0, x < 0` has `lim_{x→0^{−}} sgn(x) = −1` and `lim_{x→0^{+}} sgn(x) = 1`; hence `x = 0` is a jump discontinuity with jump 2. |
| BasicAnalysis.counterexample.essentialSin | counterexample | Essential discontinuity: `sin(1/x)` at 0 | BasicAnalysis.def.essentialDiscontinuity | Define `f(x) = sin(1/x)` for `x ≠ 0`. By Heine's corollary, taking `x_n = 1/(nπ) → 0` gives `f(x_n) = 0`, while `y_n = 1/(π/2 + 2nπ) → 0` gives `f(y_n) = 1`. Hence neither one-sided limit exists, and 0 is an essential discontinuity. |
| BasicAnalysis.subsec.closedIntervalProps | subsection | Continuous functions on a closed interval | BasicAnalysis.sec.functionLimits | Four cornerstone theorems for `f ∈ C([a,b])`: boundedness, attainment of extrema, intermediate value theorem, and uniform continuity (Cantor's theorem). |
| BasicAnalysis.ctxt.closedInterval | context | Ambient closed interval | BasicAnalysis.subsec.closedIntervalProps | Fix real numbers `a < b` and a continuous function `f : [a,b] → ℝ`. All statements in this subsection concern this setting. |
| BasicAnalysis.thm.boundedness | theorem | Boundedness theorem | BasicAnalysis.subsec.closedIntervalProps | If `f : [a,b] → ℝ` is continuous, then `f` is bounded on `[a,b]`, i.e. there exists `M > 0` with `|f(x)| ≤ M` for all `x ∈ [a,b]`. |
| BasicAnalysis.proof.boundedness | proof | Proof via Bolzano–Weierstrass | BasicAnalysis.thm.boundedness | Suppose `f` were unbounded; then for each `n ∈ ℕ` there is `x_n ∈ [a,b]` with `|f(x_n)| > n`. Since `[a,b]` is bounded, by Bolzano–Weierstrass a subsequence `x_{n_k} → x^* ∈ [a,b]`. Continuity of `f` at `x^*` gives `f(x_{n_k}) → f(x^*) ∈ ℝ`, contradicting `|f(x_{n_k})| > n_k → ∞`. |
| BasicAnalysis.thm.extremeValue | theorem | Extreme value theorem | BasicAnalysis.subsec.closedIntervalProps | If `f : [a,b] → ℝ` is continuous, then there exist `x_*, x^* ∈ [a,b]` such that `f(x_*) = inf_{x∈[a,b]} f(x)` and `f(x^*) = sup_{x∈[a,b]} f(x)`. |
| BasicAnalysis.proof.extremeValue | proof | Proof via approximating sequence and Bolzano–Weierstrass | BasicAnalysis.thm.extremeValue | Let `M = sup f([a,b])`, which is finite by the boundedness theorem. Pick `x_n ∈ [a,b]` with `f(x_n) → M`. Extract a convergent subsequence `x_{n_k} → x^* ∈ [a,b]` by Bolzano–Weierstrass; continuity gives `f(x^*) = lim f(x_{n_k}) = M`. The infimum case is symmetric. |
| BasicAnalysis.thm.intermediateValue | theorem | Intermediate value theorem | BasicAnalysis.subsec.closedIntervalProps | Let `f : [a,b] → ℝ` be continuous. For every real number `y` strictly between `f(a)` and `f(b)`, there exists `c ∈ (a,b)` with `f(c) = y`. |
| BasicAnalysis.proof.intermediateValue | proof | Proof by bisection | BasicAnalysis.thm.intermediateValue | WLOG `f(a) < y < f(b)`; consider `g(x) = f(x) − y`, so `g(a) < 0 < g(b)`. Bisect `[a,b]`: at each step pick the half whose endpoints have `g` of opposite sign, producing nested closed intervals `[a_n, b_n]` of length `(b−a)/2^n` with `g(a_n) ≤ 0 ≤ g(b_n)`. By the nested interval principle they share a unique point `c`, and `a_n, b_n → c`. Continuity of `g` yields `g(c) = lim g(a_n) ≤ 0` and `g(c) = lim g(b_n) ≥ 0`, so `g(c) = 0`, i.e. `f(c) = y`. |
| BasicAnalysis.corollary.rootExistence | corollary | Root existence for sign-changing continuous functions | BasicAnalysis.thm.intermediateValue | If `f : [a,b] → ℝ` is continuous and `f(a) f(b) < 0`, then there exists `c ∈ (a,b)` with `f(c) = 0`. |
| BasicAnalysis.corollary.continuousImageInterval | corollary | Continuous image of an interval is an interval | BasicAnalysis.thm.intermediateValue | If `I ⊆ ℝ` is an interval and `f : I → ℝ` is continuous, then `f(I)` is also an interval. |
| BasicAnalysis.def.uniformlyContinuous | definition | Uniformly continuous | BasicAnalysis.subsec.closedIntervalProps | `f : I → ℝ` is *uniformly continuous on `I`* iff for every `ε > 0` there exists `δ > 0` such that for all `x, y ∈ I` with `|x − y| < δ`, `|f(x) − f(y)| < ε`. The δ depends only on ε, not on the point. |
| BasicAnalysis.remark.uniformVsPointwise | remark | Uniform vs. pointwise continuity | BasicAnalysis.def.uniformlyContinuous | Every uniformly continuous function is continuous. The converse fails on non-compact domains: e.g. `f(x) = 1/x` on `(0,1)` and `f(x) = x^2` on ℝ are continuous but not uniformly continuous. |
| BasicAnalysis.counterexample.nonUniform | counterexample | `x^2` is not uniformly continuous on ℝ | BasicAnalysis.def.uniformlyContinuous | Take `x_n = n` and `y_n = n + 1/n`; then `|x_n − y_n| = 1/n → 0`, yet `|x_n^2 − y_n^2| = 2 + 1/n^2 ≥ 2`. Hence no single `δ` works for `ε = 1`. |
| BasicAnalysis.thm.cantorUniform | theorem | Cantor's theorem (uniform continuity on a closed interval) | BasicAnalysis.subsec.closedIntervalProps | If `f : [a,b] → ℝ` is continuous, then `f` is uniformly continuous on `[a,b]`. |
| BasicAnalysis.proof.cantorUniform | proof | Proof by contradiction and Bolzano–Weierstrass | BasicAnalysis.thm.cantorUniform | Suppose not: there exists `ε_0 > 0` such that for every `n ∈ ℕ` one finds `x_n, y_n ∈ [a,b]` with `|x_n − y_n| < 1/n` yet `|f(x_n) − f(y_n)| ≥ ε_0`. Extract `x_{n_k} → x^* ∈ [a,b]`; then also `y_{n_k} → x^*`. Continuity of `f` at `x^*` gives `f(x_{n_k}), f(y_{n_k}) → f(x^*)`, hence `|f(x_{n_k}) − f(y_{n_k})| → 0`, contradicting `≥ ε_0`. |
| BasicAnalysis.remark.compactnessMoral | remark | Compactness is the underlying reason | BasicAnalysis.thm.cantorUniform | The boundedness theorem, extreme value theorem, and Cantor's theorem all rely on the same key fact: `[a,b]` is sequentially compact, so every infinite sequence has a convergent subsequence with limit in `[a,b]`. This is the source from which the whole subsection flows. |
