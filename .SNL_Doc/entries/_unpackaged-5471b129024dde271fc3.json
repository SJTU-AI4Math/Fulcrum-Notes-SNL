{
  "format": "snl-entry",
  "version": 1,
  "package": "_unpackaged",
  "entry": {
    "id": "BasicAnalysis.def.seqWindow",
    "kind": "definition",
    "title": {
      "type": "i18n",
      "default_language": "en",
      "values": {
        "en": "Sequence with an Index Window",
        "zh-CN": "带指标窗口的数列"
      }
    },
    "content": {
      "snl": "def(Type.judge(@A, Analysis.Seq), Analysis.mkSeq(Analysis.map(A), Analysis.init(A), Analysis.final(A)))",
      "markdown": {
        "type": "i18n",
        "default_language": "en",
        "values": {
          "en": "A sequence `A : Seq` consists of a total map `A.map : ℕ → ℝ`, a first index `A.init : ℕ`, and an end marker `A.final : WithTop ℕ` holding one past the last index. `A.final = none` marks an infinite sequence; a finite sequence occupies the index range `[A.init, A.final)`.\n\nThe window is what lets a single type carry both finite and infinite sequences, and it is why the limit definition must explicitly require `A.final = none`: asking for the limit of a finite sequence is a category error rather than a false statement.",
          "zh-CN": "数列 `A : Seq` 由一个全映射 `A.map : ℕ → ℝ`、首指标 `A.init : ℕ` 和终止标记 `A.final : WithTop ℕ` 组成，后者保存末指标的后一个位置。`A.final = none` 标记无限数列；有限数列占据指标范围 `[A.init, A.final)`。\n\n这个窗口使同一个类型能够同时承载有限数列和无限数列，也正因如此，极限定义必须显式要求 `A.final = none`：询问有限数列的极限属于范畴错误，而非一个假命题。"
        }
      }
    },
    "contribution_info": null,
    "pointer": null,
    "package": "_unpackaged"
  }
}
